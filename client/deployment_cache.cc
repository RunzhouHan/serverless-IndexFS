#include "client/deployment_cache.h"



// void* DeploymentCache::DeploymentCache() {
//     // return reinterpret_cast<void*> (new Map);
    
// }

// void deployment_cache_free(void* cache) {
//     delete(cache);
// }

// void cache_put(void* map,  char* path, int deployment_id) {
//     Cache* m = reinterpret_cast<Cache*> (map);
//     m->insert(std::pair<std::string, int>(k, v));
// }

// int cache_get(void* map, void* path) {
//     Cache* m = reinterpret_cast<Cache*> (map);
//     std::string str(path);
//     return m->find(path);
// }

